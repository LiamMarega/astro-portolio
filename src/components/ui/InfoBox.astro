---
// InfoBox.astro - Animated info box displaying skills and technologies
---

<div
  class="w-full max-w-[90%] md:max-w-[85%] lg:max-w-[80%] xl:max-w-[75%] relative mt-8 sm:mt-12 md:mt-16"
>
  <div
    class="relative flex items-center px-4 md:px-8 py-6 md:py-8 bg-white rounded-r-full animate-slide-in"
  >
    <!-- Avatar image -->
    <img
      class="w-24 h-24 sm:w-32 sm:h-32 md:h-48 md:w-48 lg:h-60 lg:w-60 ml-auto mr-4 sm:mr-8 md:mr-16 lg:mr-20 object-contain hover:scale-105 transition-transform duration-300 animate-slide-in-with-container"
      alt="LiamMarega-avatar"
      src="/images/avatar-withoutbg.png"
    />

    <!-- Mobile developer bubble -->
    <div
      class="bubble-card absolute -top-16 sm:-top-20 md:-top-24 left-4 sm:left-10 md:left-20
      bg-green-200 delay-1 flex-center"
    >
      <h3
        class="text-black text-sm sm:text-base md:text-lg lg:text-xl font-bold"
      >
        MOBILE DEVELOPER
      </h3>
      <span class="hidden sm:block text-black text-xs md:text-sm">
        Professional iOS and Android developer
      </span>
    </div>

    <!-- Web developer bubble -->
    <div
      class="bubble-card absolute -bottom-16 sm:-bottom-20 right-16 sm:right-24 md:right-48 lg:right-60
      bg-lime-200 delay-2 flex-center"
    >
      <h3
        class="text-black text-sm sm:text-base md:text-lg lg:text-xl font-bold"
      >
        WEB DEVELOPER
      </h3>
      <span class="hidden sm:block text-black text-xs md:text-sm">
        Developing websites with best practices
      </span>
    </div>

    <!-- Technologies card 1 -->
    <div
      class="tech-card absolute top-1/4 md:top-1/3 lg:top-40 xl:top-48 right-4 md:right-auto md:left-24 lg:left-32 xl:left-40
      rotate-6 sm:rotate-12 bg-yellow-300 delay-3"
    >
      <div class="corner-dot left-2 top-2"></div>
      <div class="corner-dot right-2 top-2"></div>
      <div class="corner-dot left-2 bottom-2"></div>
      <div class="corner-dot right-2 bottom-2"></div>
      <h3 class="text-black font-bold text-sm sm:text-base md:text-lg">
        REACT
      </h3>
      <h3 class="text-black font-bold text-sm sm:text-base md:text-lg">
        FLUTTER
      </h3>
      <h3 class="text-black font-bold text-sm sm:text-base md:text-lg">
        NEXT.JS
      </h3>
    </div>

    <!-- Technologies card 2 -->
    <div
      class="tech-card absolute hidden sm:flex top-10 sm:top-12 md:top-14 right-2 sm:-right-8 md:-right-14
      -rotate-6 sm:-rotate-12 bg-green-200 delay-4"
    >
      <div class="corner-dot left-2 top-2"></div>
      <div class="corner-dot left-2 bottom-2"></div>
      <h3 class="text-black font-bold text-sm sm:text-base md:text-lg">
        UX/UI Trends
      </h3>
      <h4 class="text-black font-bold text-sm sm:text-base md:text-lg">@'25</h4>
    </div>
  </div>
</div>

<style>
  /* Reusable bubble styles */
  .bubble-card {
    @apply size-20 sm:size-24 md:size-28 lg:size-32 xl:size-36 p-2 sm:p-3 md:p-4
      rounded-full flex flex-col items-center justify-center text-center
      animate-pop-in;
  }

  /* Reusable tech card styles */
  .tech-card {
    @apply w-20 h-24 sm:w-32 sm:h-28 md:w-40 md:h-32
      px-2 sm:px-4 md:px-6 py-3 sm:py-4
      rounded-2xl flex flex-col items-center justify-center
      animate-pop-in;
  }

  /* Corner dots for cards */
  .corner-dot {
    @apply absolute size-2 rounded-full bg-gray-600;
  }

  /* Reusable flex center utility */
  .flex-center {
    @apply flex flex-col items-center justify-center text-center;
  }

  /* Animation keyframes */
  @keyframes slideIn {
    0% {
      transform: translateX(-100%);
      opacity: 0;
    }
    100% {
      transform: translateX(0);
      opacity: 1;
    }
  }

  @keyframes popIn {
    0% {
      transform: scale(0);
      opacity: 0;
    }
    50% {
      transform: scale(1.2);
    }
    100% {
      transform: scale(1);
      opacity: 1;
    }
  }

  /* Animation classes */
  .animate-slide-in {
    animation: slideIn 1.5s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
  }

  .animate-slide-in-with-container {
    animation: none; /* Inherit parent container animation */
  }

  .animate-pop-in {
    opacity: 0;
    animation: popIn 0.6s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
  }

  .delay-1 {
    animation-delay: 1.5s;
  }

  .delay-2 {
    animation-delay: 1.7s;
  }

  .delay-3 {
    animation-delay: 1.9s;
  }

  .delay-4 {
    animation-delay: 2.1s;
  }
</style>
