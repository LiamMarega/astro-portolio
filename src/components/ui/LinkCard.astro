---
interface Props {
  title: string;
  description?: string;
  url: string;
  icon?: string;
  gradient?: string;
}

const { title, description, url, icon, gradient = "from-purple-500 to-pink-500" } = Astro.props;
---

<a 
  href={url} 
  target="_blank" 
  rel="noopener noreferrer"
  class="group block w-full max-w-xs sm:max-w-sm md:max-w-md mx-auto mb-4 transform transition-all duration-300 hover:scale-[1.02] focus:scale-[1.02] hover:-translate-y-1 focus:-translate-y-1 focus:outline-none rounded-xl sm:rounded-2xl"
  aria-label={`Visit ${title}${description ? ': ' + description : ''} (opens in new tab)`}
>
  <div class={`relative overflow-hidden rounded-xl sm:rounded-2xl bg-gradient-to-tr ${gradient} p-[1.5px] shadow-lg group-hover:shadow-[0_0_20px_rgba(255,255,255,0.3)] transition-shadow duration-300`}>
    <div class="relative bg-gray-900/95 backdrop-blur-md rounded-[11px] sm:rounded-[15px] p-4 sm:p-5 h-full transition-all duration-300 group-hover:bg-gray-900/80">
      <div class="flex items-center gap-3 sm:gap-4">
        {icon && (
          <div class="flex-shrink-0">
            <div class="w-10 h-10 sm:w-12 sm:h-12 rounded-full bg-white/10 flex items-center justify-center text-lg sm:text-2xl" aria-hidden="true">
              {icon}
            </div>
          </div>
        )}
        <div class="flex-1 min-w-0">
          <h3 class="text-base sm:text-lg font-semibold text-white truncate group-hover:text-transparent group-hover:bg-clip-text group-hover:bg-gradient-to-r group-hover:from-white group-hover:to-gray-300 motion-reduce:group-hover:text-white">
            {title}
          </h3>
          {description && (
            <p class="text-xs sm:text-sm text-gray-300 mt-1 line-clamp-2 leading-relaxed">
              {description}
            </p>
          )}
        </div>
        <div class="flex-shrink-0">
          <svg 
            class="w-5 h-5 text-white/50 group-hover:text-white group-hover:translate-x-1 transition-all duration-300" 
            fill="none" 
            stroke="currentColor" 
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </div>
      </div>
    </div>
  </div>
</a>

<style>
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* Ensure proper touch targets on mobile */
  @media (max-width: 640px) {
    a {
      min-height: 44px;
      touch-action: manipulation;
    }
  }

  /* Reduce motion for accessibility */
  @media (prefers-reduced-motion: reduce) {
    a {
      transition: none;
      transform: none;
    }
    
    a:hover,
    a:focus {
      transform: none;
    }
    
    .group:hover h3,
    .group:focus h3 {
      background: none;
      -webkit-background-clip: unset;
      background-clip: unset;
      color: white;
    }
  }

  /* High contrast mode support */
  @media (prefers-contrast: high) {
    a {
      border: 2px solid currentColor;
    }
    
    .group:hover .bg-black\/90,
    .group:focus .bg-black\/90 {
      background-color: rgb(0 0 0 / 0.95);
    }
    
    h3, p {
      color: white !important;
    }
  }
  
  /* Enhanced focus indicators */
  a:focus-visible {
    outline: 3px solid #ffffff;
    outline-offset: 3px;
    box-shadow: 0 0 0 6px rgba(255, 255, 255, 0.3);
  }
  
  /* Skip link support */
  a:focus:not(:focus-visible) {
    outline: none;
  }

  /* Performance optimization */
  a {
    will-change: transform;
  }

  a:not(:hover):not(:focus) {
    will-change: auto;
  }
</style>